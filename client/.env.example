# Client build-time config for a separately hosted frontend (e.g. Render Static Site).
# Point this at your deployed server's /api URL. Include the /api suffix.
# Leave unset for local dev or when the server serves the built client (single service).
VITE_API_URL=https://your-server.onrender.com/api
